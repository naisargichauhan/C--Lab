//reverse a strng
#include<stdio.h>
int main()
{
    char str1[20];
    char str2[20];
    int i=0;
    int length=0;

     printf("Enter the string to be reverserd: ");
    gets(str1);

     length= strlen(str);

     for(i=0;i<lrmgth;i++)
     {
         str2[i]=str[length-i-1];

     }
     rev[i] ='\0';

    printf("Reversed string: %s\n",rev);
    return 0;

    }
